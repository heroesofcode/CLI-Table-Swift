vertical_whitespace:
    severity: error

opt_in_rules:
  - empty_count
  - comma

excluded: # paths to ignore during linting. Takes precedence over `included`.
  - Pods
  - Carthage
  - Fastlane
  - Tests
